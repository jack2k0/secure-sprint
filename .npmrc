# Scope mapping only. Never commit a token here.
# Authenticate separately:
#   local: npm login --scope=@jack2k0 --registry=https://npm.pkg.github.com
#   CI:    GH_PKG_TOKEN secret, written to a runner-local .npmrc
@jack2k0:registry=https://npm.pkg.github.com
